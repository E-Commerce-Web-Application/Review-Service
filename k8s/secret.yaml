apiVersion: v1
kind: Secret
metadata:
  name: review-service-secret
  namespace: review-service
type: Opaque
stringData:
  # Replace this with the real MongoDB URI at deploy time.
  MONGO_URI: "<REPLACE_WITH_SECURE_URI>"